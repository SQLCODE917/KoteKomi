schema_version = 2
task_id = "verification-plan-receipt-scope-474179deda5f"
title = "Verification Plan Receipt Scope"
status = "ready_for_terra_high"
series_id = "agent-harness"
task_class = "repository-tooling"
model_profile = "terra-high-v1"
baseline_revision = "0b5b2233a835355a55022eef01a62edecb67f0c2"
tdd_path = "docs/2026-08-21-verification-plan-receipt-scope.md"
tdd_sha256 = "474179deda5f462f08e168a38e75011825d337723094e5a10bbba539ee6fe0ae"
goal = "Allow verification planning to exclude only a trusted candidate receipt-only commit."
depends_on = []
allowed_paths = [
  "packages/devtools/src/kotekomi_devtools/verification_plan.py",
  "packages/devtools/tests/acceptance/test_verification_plan_contract.py",
  "packages/devtools/tests/unit/test_verification_plan.py"
]
reference_paths = [
  "docs/2026-08-18-tdd-feature-branch-verification-receipt.md",
  "packages/devtools/src/kotekomi_devtools/candidate_verifier.py",
  "packages/devtools/src/kotekomi_devtools/task_scope.py",
  "packages/devtools/tests/acceptance/test_task_scope_audit_contract.py"
]
stop_conditions = [
  "the implementation changes the Task Manifest schema",
  "the implementation adds a receipt-directory path-prefix allowlist",
  "the implementation accepts a receipt that does not bind its one parent"
]

[readiness]
authority = "The Verification Plan Receipt Scope TDD defines this task."
contract_family = "agent-harness"
dominant_outcome = "A trusted receipt-only commit does not block verification planning."
failure_policy = "An invalid receipt path remains an uncovered changed path."
legacy_disposition = "Existing scope and budget receipt handling remains unchanged."
negative_proof = "The planner does not trust a receipt directory, a multi-file commit, or a mismatched receipt."
public_entry_point = "kotekomi-agent verification-plan"
scope_policy = "Verification planner receipt recognition and focused planner tests only."
side_effect_boundary = "The command writes only its requested verification plan reports."
unresolved_decisions = []

[budget]
maximum_production_files = 1
maximum_test_files = 2
maximum_production_diff_lines = 350

[[protected_artifacts]]
kind = "leaf-tdd"
path = "docs/2026-08-21-verification-plan-receipt-scope.md"
sha256 = "474179deda5f462f08e168a38e75011825d337723094e5a10bbba539ee6fe0ae"

[[acceptance]]
id = "verification-plan-receipt-scope-contract"
argv = ["uv", "run", "pytest", "-p", "no:cacheprovider", "packages/devtools/tests/acceptance/test_verification_plan_contract.py"]
timeout_seconds = 120
profile = "portable-local"

[[acceptance]]
id = "verification-plan-unit-contract"
argv = ["uv", "run", "pytest", "-p", "no:cacheprovider", "packages/devtools/tests/unit/test_verification_plan.py"]
timeout_seconds = 120
profile = "portable-local"

[[acceptance]]
id = "task-scope-receipt-contract-retained"
argv = ["uv", "run", "pytest", "-p", "no:cacheprovider", "packages/devtools/tests/acceptance/test_task_scope_audit_contract.py", "packages/devtools/tests/acceptance/test_task_budget_audit_contract.py"]
timeout_seconds = 120
profile = "portable-local"

[[acceptance]]
id = "repository-static-checks"
argv = ["uv", "run", "ruff", "check"]
timeout_seconds = 120
profile = "portable-local"

[[acceptance]]
id = "repository-type-checks"
argv = ["uv", "run", "pyright"]
timeout_seconds = 240
profile = "portable-local"
