schema_version = 2
task_id = "dr-3-exact-lexical-diagnostic-cli-03daf379ca7d"
title = "DR-3 Exact-Lexical Diagnostic CLI"
status = "ready_for_terra_high"
series_id = "derived-retrieval"
task_class = "wiring"
model_profile = "terra-high-v1"
baseline_revision = "1deac76e230127f60a24779e06cc926994144390"
tdd_path = "docs/2026-08-22-dr-3-exact-lexical-diagnostic-cli.md"
tdd_sha256 = "03daf379ca7d81bf44ebdb373d113be8c3874920f496e6ebd275bc5e80925f61"
goal = "Expose the existing exact-lexical diagnostic retrieval branch through the public CLI."
depends_on = []
allowed_paths = [
  "packages/pipelines/src/kotekomi_pipelines/cli.py",
  "packages/pipelines/tests/test_document_retrieval.py",
  "packages/pipelines/tests/test_source_add_file.py"
]
reference_paths = [
  "docs/2026-08-21-document-hybrid-activation.md",
  "packages/pipelines/src/kotekomi_pipelines/cli.py",
  "packages/pipelines/tests/test_document_retrieval.py",
  "packages/pipelines/tests/test_source_add_file.py"
]
stop_conditions = [
  "the implementation changes normal hybrid retrieval behavior",
  "the implementation changes exact guard, lexical ranking, or hybrid fusion behavior",
  "the implementation adds an embedding profile to the source-ingest fixture"
]

[readiness]
authority = "The DR-3 Exact-Lexical Diagnostic CLI TDD defines this task."
contract_family = "derived-retrieval"
dominant_outcome = "The public Pipeline CLI exposes the existing exact-lexical diagnostic channel."
failure_policy = "The CLI rejects an embedding profile unless the channel is semantic."
legacy_disposition = "The unqualified command remains the DR-3 normal hybrid command."
negative_proof = "The task does not change retrieval policy, derived records, or embedding configuration."
public_entry_point = "kotekomi retrieval build-document and kotekomi retrieval query"
scope_policy = "CLI channel validation and focused Pipeline tests only."
side_effect_boundary = "The commands create only existing disposable retrieval projections and query records."
unresolved_decisions = []

[budget]
maximum_production_files = 1
maximum_test_files = 2
maximum_production_diff_lines = 250

[[protected_artifacts]]
kind = "leaf-tdd"
path = "docs/2026-08-22-dr-3-exact-lexical-diagnostic-cli.md"
sha256 = "03daf379ca7d81bf44ebdb373d113be8c3874920f496e6ebd275bc5e80925f61"

[[acceptance]]
id = "diagnostic-cli-contract"
argv = ["uv", "run", "pytest", "packages/pipelines/tests/test_document_retrieval.py", "packages/pipelines/tests/test_source_add_file.py"]
timeout_seconds = 360
profile = "portable-local"

[[acceptance]]
id = "repository-static-checks"
argv = ["uv", "run", "ruff", "check"]
timeout_seconds = 120
profile = "portable-local"

[[acceptance]]
id = "repository-type-checks"
argv = ["uv", "run", "pyright"]
timeout_seconds = 240
profile = "portable-local"
