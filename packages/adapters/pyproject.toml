[project]
name = "kotekomi-adapters"
version = "0.1.0"
description = "KoteKomi Adapters."
requires-python = ">=3.12,<3.13"
dependencies = [
  "docling>=2,<3",
  "kotekomi-application",
  "kotekomi-domain",
  "networkx>=3.4,<4",
  "onnxruntime>=1.23,<2",
]

[tool.uv.sources]
kotekomi-application = { workspace = true }
kotekomi-domain = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/kotekomi_adapters"]
