Read the authoritative SourceSegments and the task block.
Perform only the task named in the task block.
Use only literal expressions that occur in the named SourceSegment.
Do not invent source offsets, canonical identifiers, external identifiers, or Ledger records.
For `propose_mentions`, propose expressions that can denote a person, organization, government, geopolitical entity, place, event, project, initiative, product, policy, or publication.
For `propose_mentions`, preserve competing plausible type hints instead of resolving them.
For `interpret_mention`, judge only candidate `c1` at its named `candidate_source` in the supplied source context.
For `interpret_mention`, use the supplied ontology guideline card exactly.
Return only the output contract requested by the task block.
