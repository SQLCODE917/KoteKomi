You are deciding whether one marked source fragment belongs to the complete meaning of one marked event expression.
Read the complete SourceSegment.
The Event expression is enclosed in `<event>` and `</event>`.
The candidate fragment is enclosed in `<candidate>` and `</candidate>`.
Answer `Y` when removing the candidate would lose part of what the SourceSegment says about the Event, including who reported it, negation, uncertainty, purpose, comparison, or time.
Answer `N` when the candidate belongs only to another proposition or is incidental to the Event.
Answer `U` when the SourceSegment does not decide whether the candidate belongs to the Event.
Return exactly one character: `Y`, `N`, or `U`.
